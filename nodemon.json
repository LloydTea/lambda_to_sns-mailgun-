{
    "watch": ["src"],
    "ext": "ts,js,json",
    "ignore": ["node_modules", "coverage", "dist"],
    "exec": "sls deploy -s dev",
    "restartable": "rs",
    "env": {
      "NODE_ENV": "development"
    }
  }